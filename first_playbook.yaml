---
 - name : This Is MY First Playbook
   hosts : dev
   tasks : 
    - name : create a group
      group :
           name : grras
           state : present

    - name : create a user
      user :
           name : rajat
           state : present
           groups : grras
